# 🏗️ 에어플로우 분리형(Separated) 아키텍처 3대장
## 1단계: local-separated (단일 서버 프로세스 분리형) ➔ 💡 현재 유저님 단계!
지휘관(Executor): LocalExecutor

구조: 컴퓨터는 딱 1대만 씁니다. 하지만 내부 장부는 강력한 PostgreSQL로 바꾸고, 멀티 푸티 세션을 열어 webserver, scheduler, fastapi-api 프로세스를 완전히 독립된 방으로 격리해서 실행합니다.

특징: 서버 1대 가격으로 실무형 아키텍처의 프로세스 격리 성능을 100% 맛볼 수 있는 가성비 최강의 세팅입니다.

## 2단계: distributed-celery (다중 서버 분산 분리형)
지휘관(Executor): CeleryExecutor

구조: 컴퓨터를 최소 3~4대 이상 사옵니다. 1번 컴퓨터는 사장님(스케줄러), 2번은 전광판(웹서버)을 띄우고, 3번과 4번 컴퓨터는 24시간 대기하는 전문 일꾼 로봇(Celery Worker) 전용 컴퓨터로 물리적으로 컴퓨터 대수를 찢어버립니다. 중간에 일 지시서를 배달할 Redis라는 중개 큐 서버가 추가로 필요합니다.

특징: 데이터 노가다 양이 너무 많아서 컴퓨터 1대의 CPU/RAM 사양으로는 감당이 안 될 때, 컴퓨터 대수를 늘려서 해결하는 방식입니다.

## 3단계: cloud-native-k8s (클라우드 컨테이너 분리형)
지휘관(Executor): KubernetesExecutor

구조: 대기업들이 쓰는 쿠버네티스(Kubernetes) 환경 위에 올립니다. 평소에는 일꾼 컴퓨터가 대수 0대로 완전히 놀고 있다가, 스케줄러가 일을 시키는 순간에만 가상 컴퓨터 컨테이너(Pod)를 1대 순식간에 조립해서 일을 시키고, 일이 끝나면 컴퓨터 자체를 폭파해서 증발시킵니다.

특징: 자원을 극단적으로 아낄 수 있어서 AWS, GCP 같은 클라우드 환경을 쓰는 대기업 실무의 최종 보스 아키텍처입니다.
